{
  "name": "ghq-fzf",
  "scripts": {
    "compile": "bun build ./cli.ts --compile --outfile _ghq-fzf",
    "local": "bun run ./cli.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.2.21"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
